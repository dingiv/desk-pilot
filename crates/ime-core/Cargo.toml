[package]
name = "ime-core"
version = "0.1.0"
edition = "2021"
description = "swift-ime pure logic crate: trie matcher, snippet expander, middleware dispatcher, zero OS dependencies"

[dependencies]
tracing = { workspace = true }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
