[package]
name = "familiar-shell"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
# Intentionally framework-agnostic: `input_region::apply` takes raw wayland
# pointers and `gnome_ext::handshake` is pure std, so this crate is reusable by
# any winit/iced/raw-wayland desk-pilot app (geek-familiar, swift-ime).
# When the wl_region FFI is filled in (task #44), add: wayland-client, wayland-protocols.
